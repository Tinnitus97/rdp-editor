<Project Sdk="Microsoft.NET.Sdk">

  <!--
    Prueft die Dienste ohne Oberflaeche.

    Kein Testrahmen und kein net10.0-windows: Die geprueften Dateien kommen
    ohne Avalonia und ohne Windows-eigene Aufrufe aus, deshalb laeuft die
    Pruefung auf einem Linux-Laeufer in Sekunden.

    Genau dafuer steht die Auswahllogik (MonitorSelection) getrennt von der
    Bildschirmermittlung (MonitorScan): Was entschieden wird, ist pruefbar,
    ohne dass ein Bildschirm angeschlossen sein muss.
  -->
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <LangVersion>latest</LangVersion>
    <RootNamespace>RdpEditorTests</RootNamespace>
    <AssemblyName>RdpEditorTests</AssemblyName>
    <EnableDefaultCompileItems>false</EnableDefaultCompileItems>
    <InvariantGlobalization>true</InvariantGlobalization>
  </PropertyGroup>

  <ItemGroup>
    <Compile Include="Program.cs" />
    <Compile Include="../../Services/RdpDocument.cs" />
    <Compile Include="../../Services/RdpCatalog.cs" />
    <Compile Include="../../Services/MonitorSelection.cs" />
  </ItemGroup>

</Project>
